# Hospital Management System (Personal)

Local development quick start:

1. Install dependencies

```powershell
npm install
```

2. Run development server (auto-reloads using `tsx`)

```powershell
npm run dev
```

3. Build for production

```powershell
npm run build
npm start
```

Health check: `GET /health` returns a small JSON status.
